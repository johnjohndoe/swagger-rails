%h1= "Edit Operation in #{@api.path}"
= simple_nested_form_for @operation, :url => admin_doc_api_operation_path(@doc, @api, @operation), :html => { :class => 'form-horizontal' } do |f|
  = render :partial => 'form', :object => f